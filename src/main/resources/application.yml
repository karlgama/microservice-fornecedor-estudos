server:
  port: 8081

  spring:
    datasource:
      url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/fornecedor
      username: user
      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: create

      #show-sql: true